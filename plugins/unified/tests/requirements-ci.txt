# Core testing
pytest>=7.0
pytest-cov>=4.0

# Data processing
numpy>=1.24
networkx>=3.0

# Embedding model (CPU-only for CI — no CUDA needed since tests mock GPU)
torch>=2.0 --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=2.2

# LangChain (document loading)
langchain>=0.1
langchain-community>=0.0

# HTTP (consolidation & relations)
httpx>=0.24

# Scikit-learn (community detection, similarity)
scikit-learn>=1.3

# Progress bars (document loader)
tqdm>=4.65
